apiVersion: v1
kind: Service
metadata:
  name: {{ include "certificate-validate.fullname" . }}
  labels:
    {{- include "certificate-validate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "certificate-validate.selectorLabels" . | nindent 4 }}

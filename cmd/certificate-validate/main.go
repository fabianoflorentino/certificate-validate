package main

import (
	"github.com/fabianoflorentino/certificate-validate/internal/cmd"
)

// main is the entry point of the application. It executes the command-line interface (CLI) defined in the cmd package.
func main() {
	cmd.Execute()
}
